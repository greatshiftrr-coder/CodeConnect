name: Build and Deploy

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build-and-test:
    name: Build & Verify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Type check & Lint
        run: npm run lint

      - name: Build Next.js app
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}

  # ------------------------------------------------------------------
  # OPTION A: Deploy to Vercel (Recommended for Next.js Full-Stack)
  # Uncomment this job and add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID in GitHub Repository Secrets
  # ------------------------------------------------------------------
  # deploy-vercel:
  #   name: Deploy to Vercel
  #   needs: build-and-test
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Deploy to Vercel Action
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
  #         vercel-args: '--prod'

  # ------------------------------------------------------------------
  # OPTION B: Deploy to Google Cloud Run
  # Uncomment this job and configure GCP_SA_KEY & GCP_PROJECT_ID in GitHub Repository Secrets
  # ------------------------------------------------------------------
  # deploy-cloud-run:
  #   name: Deploy to Cloud Run
  #   needs: build-and-test
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Authenticate to Google Cloud
  #       uses: google-github-actions/auth@v2
  #       with:
  #         credentials_json: ${{ secrets.GCP_SA_KEY }}
  #
  #     - name: Set up Cloud SDK
  #       uses: google-github-actions/setup-gcloud@v2
  #
  #     - name: Build and Push Docker Image
  #       run: |
  #         gcloud auth configure-docker
  #         docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/codeconnect:${{ github.sha }} .
  #         docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/codeconnect:${{ github.sha }}
  #
  #     - name: Deploy to Cloud Run
  #       run: |
  #         gcloud run deploy codeconnect \
  #           --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/codeconnect:${{ github.sha }} \
  #           --platform managed \
  #           --region us-central1 \
  #           --allow-unauthenticated
